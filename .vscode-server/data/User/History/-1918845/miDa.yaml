---
- hosts: all
  become: true
  tasks:
    - name: Install apache
      package:
        name: 
        - "{{apache_package}}"
        - "{{php_package}}"
        state: latest
        update_cache: yes
- hosts: dbservers
  become: true
  tasks:
    - name: install maria dbservers
      package: 
        name: maria-server
        state: latest